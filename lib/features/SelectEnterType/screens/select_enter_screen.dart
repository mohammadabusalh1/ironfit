import 'package:flutter/material.dart';
import 'package:ironfit/features/SelectEnterType/widgets/select_enter_body.dart';

class SelectEnterScreen extends StatelessWidget {
  const SelectEnterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SelectEnterBody(),
    );
  }
}
